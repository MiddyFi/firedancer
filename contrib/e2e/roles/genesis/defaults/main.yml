---
cluster_type: devnet

workdir: "{{ e2e_workdir }}"
bindir: "{{ e2e_workdir }}/solana/{{ labs_repo_version }}"

keys_dir: "{{ workdir }}/bootstrap-keys"
genesis_cfg: "{{ workdir }}/genesis.yml"
ledger: "{{ workdir }}/ledger"
faucet: "{{ keys_dir }}/faucet.json"
authority: "{{ keys_dir }}/authority.json"
bootstrap_validator_lamports: 1000000000000
bootstrap_validator_stake_lamports: 322828800000
faucet_lamports: 5000000000000000
snapshot_interval: 200
ledger_size: 100000000
